hii this is git branching means create a new branch inside or you
 can say file inside the folder 
for which we use touch branch name and if touch is not working 
then we use New_Item branchname for ex about.html,git.sh whatever 
you want to give you can and the this will create a new branch inside 
the running foder 


hdahgfafadvhchjadvgfdhfj